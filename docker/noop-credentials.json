{
  "_comment": "NOT A CREDENTIAL. A placeholder so docker compose can start when GOOGLE_CREDENTIALS_PATH is unset - a bind mount to a missing path fails the whole stack, and a teammate who does not need Drive should not have to obtain a key to run the project. GOOGLE_DRIVE_ENABLED=false is what actually turns the feature off; this file just gives the mount something to point at.",
  "type": "placeholder"
}
