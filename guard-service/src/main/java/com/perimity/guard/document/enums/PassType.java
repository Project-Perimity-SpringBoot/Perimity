package com.perimity.guard.document.enums;

/**
 * Which kind of QR was physically scanned.
 * Mirrors the enum of the same name in gatepass-service.
 */
public enum PassType {
    DAILY,
    EVENT
}
