<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- 3.3.5 exactly. Spring Initializr defaults to a newer version; override it.
         Every Perimity service must be on the same Boot minor, or the shared
         JwtAuthFilter from Day 4 will not drop into all six. -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.5</version>
        <relativePath/>
    </parent>

    <groupId>com.perimity</groupId>
    <artifactId>user-service</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>user-service</name>
    <description>Student and faculty profiles, departments and documents</description>

    <properties>
        <java.version>17</java.version>
        <spring-cloud.version>2023.0.6</spring-cloud.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Cloud 2023.0.x is the release train for Spring Boot 3.3.x.
                 Never pin individual Spring Cloud versions - this BOM keeps them
                 consistent with each other and with Boot. -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
    <!-- ===== Service discovery (Eureka client) ===== -->
    <!-- Registers this service with discovery-server on startup and heartbeats
         every 10s. Optional at runtime: eureka.client.enabled=false turns it off
         and the service behaves exactly as it did before. -->
    <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
    </dependency>

    <!-- ===== Declarative HTTP clients (OpenFeign) ===== -->
    <!-- Only in services that CALL peers. Feign resolves
         @FeignClient(name = "campus-service") through Eureka, so no URL
         appears in the code. -->
    <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-starter-openfeign</artifactId>
    </dependency>

    <!-- Required for the 'fallback' attribute on @FeignClient to be honoured.
         Without a circuit breaker on the classpath Feign throws on failure and
         the fail-soft behaviour is silently lost. -->
    <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-starter-circuitbreaker-resilience4j</artifactId>
    </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.6.0</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Day 7 - Spring Security.
             Adding this locks EVERY endpoint by default, including /ping and
             Swagger. config/SecurityConfig.java is what re-opens them; if you
             remove that class, this dependency will make the service look
             completely broken. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- Day 7 - JWT.
             user-service only VERIFIES tokens; auth-service is the only service
             that issues them. jjwt-impl and jjwt-jackson are runtime scope on
             purpose: compiling against them instead of jjwt-api is how you end
             up coupled to an internal class that changes between releases.
             Version 0.12.6 must match auth-service. -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.12.6</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.12.6</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>0.12.6</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Day 7 - lets @WithMockUser and the security filter chain be tested. -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Test-only in-memory database, so a context test does not need a
             running Postgres container. -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- ========== Day 9: object storage ==========
             The S3 SDK is only USED when perimity.storage.type=s3. With the
             default (local) it sits unused on the classpath, so nobody needs an
             AWS account or a network connection to run this service.

             2.28.16 matches campus-service. Two AWS SDK versions in one repo
             pull in two copies of the shared core and the classpath winner is
             whichever Maven happens to resolve first. -->
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>s3</artifactId>
            <version>2.28.16</version>
        </dependency>

        <!--
          AMQP - consumes user.created from auth-service and provisions the
          profile for a new STUDENT or FACULTY account.

          Before this, the profile was created by a second API call from the
          browser after the account was made. Any account whose second call did
          not happen could sign in with no profile, and nothing in the product
          could create the missing row afterwards.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-amqp</artifactId>
        </dependency>

        <!-- ============================================================
             Apache POI - reads the Google Form responses .xlsx for bulk
             student onboarding. See docs/BULK_STUDENT_ONBOARDING.md.

             5.3.0 matches gatepass-service exactly. Two POI versions in one
             repo is the same class of problem as two AWS SDK versions: the
             classpath winner is whichever Maven resolves first.

             poi-ooxml pulls in poi transitively, and both are declared so the
             versions stay locked together - a mismatch between them produces
             NoSuchMethodError at RUNTIME, not at compile time.
             ============================================================ -->
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi</artifactId>
            <version>5.3.0</version>
        </dependency>
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml</artifactId>
            <version>5.3.0</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
